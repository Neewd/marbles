import { createAction, props } from '@ngrx/store';
import { ModalEntity } from './modal.models';

export const openModal = createAction('[Modal Page] Init');
export const closeModal = createAction('[Modal Page] Init');
