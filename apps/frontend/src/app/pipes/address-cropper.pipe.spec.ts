import { AddressCropperPipe } from './address-cropper.pipe';

describe('AddressCropperPipe', () => {
  it('create an instance', () => {
    const pipe = new AddressCropperPipe();
    expect(pipe).toBeTruthy();
  });
});
